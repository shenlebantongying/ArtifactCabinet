#lang rhombus

def inP = Port.Input.open_file("./data/rosalind_fibo.txt")
def i = inP.read_line().to_int()

fun fib(n):
  cond
  | n == 0: 0
  | n == 1: 1
  | ~else: fib(n - 1) + fib(n - 2)

println(fib(i))
